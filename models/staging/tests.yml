version: 2

models:
  - name: stg_raw_orders
    columns:
      - name: id
        tests: [not_null, unique]
      - name: order_id
        tests: [not_null]

  - name: stg_raw_returns
    columns:
      - name: order_id
        tests: [not_null]